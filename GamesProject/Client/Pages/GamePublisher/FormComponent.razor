<EditForm Model="@gamePublisher" OnValidSubmit="@OnValidSubmit">
    <DataAnnotationsValidator />
    <ValidationSummary />
    <fieldset disabled="@Disabled">
        <div class="form-group">
            <label>New GamePublisher </label>
            <div>
                <InputText @bind-Value="gamePublisher.Name" class="form-control" />
                <ValidationMessage For="@(()=> gamePublisher.Name)" />
            </div>
        </div>
    </fieldset>

        @if (!Disabled)
        {
            <button type="submit" class="btn btn-success btn-block">
                @ButtonText
            </button>
        }
        <GamesProject.Client.Components.BackToList Target="gamePublisher" />
</EditForm>

@code{
    [Parameter] public bool Disabled { get; set; } = false;
    [Parameter] public GamePublisher gamePublisher { get; set; }
    [Parameter] public string ButtonText { get; set; } = "Save";
    [Parameter] public EventCallback OnValidSubmit { get; set; }

}
